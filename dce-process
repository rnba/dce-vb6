#!/usr/bin/execlineb -WP

multisubstitute {
  importas -i mode mode
  importas -i file file
  importas -i t t
  importas -i n n
}
dce-print { processing $file $t $n }
dce-skip test
foreground { dce-delete-def }
ifelse -n { dce-grep -q }
{
  dce-commit
}
exec dce-process-${mode}
