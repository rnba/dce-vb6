#!/usr/bin/execlineb -WP

multisubstitute {
  importas -i mode mode
  importas -i file file
  importas -i t t
  importas -i n n
}
foreground { echo processing $file $t $n }
dce-skip test
foreground { dce-delete-def }
ifelse -n { dce-grep -q }
{
  if { dce-commit } touch go_on
}
exec dce-process-${mode}
