#!/usr/bin/execlineb -WS1

multisubstitute {
  define next $1
  importas -i dir dir
  importas -i file file
  importas -i t t
  importas -i n n
}
foreground { echo processing $file $t $n }
foreground { dce-delete-def }
ifelse -n { git grep -iqrw $n $dir }
{
  foreground { touch go_on }
  dce-commit
}
exec $next
