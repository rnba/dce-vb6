#!/usr/bin/execlineb -WP

importas -i file file
pipeline { dce-decomment $file }
pipeline { dce-symbols }
pipeline { cat -n }
pipeline { sort -b -k 2,2n -k 1,1n }
sed -E "s/^(\\s+[[:digit:]]+){2} //"
