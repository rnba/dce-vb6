a = 0 ' 'trivial'
b = "a string literal..." ' as well
c = "is not a 'comment'!" ' less so
html = Replace(s, "'", "&apos;") ' multiple literals
lessons("I'm", "You're", "We're") ' multiple 'problems'

  a = 0 ' 'trivial'
  b = "a string literal..." ' as well
  c = "is not a 'comment'!" ' less so
  html = Replace(s, "'", "&apos;") ' multiple literals
  lessons("I'm", "You're", "We're") ' multiple 'problems'
